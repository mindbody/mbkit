# Dev Documentation

```tsx
<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gridGap: 24, padding: '16px 8px' }}>
    <Card elevation={0} style={{ padding: 16 }}>
        <p>Elevation 0 has no shadow and is an escape in case you don't need shadows for some effect</p>
    </Card>
    <Card elevation={1} style={{ padding: 16 }}>
        <p>Elevation 1 is for cards and is the default</p>
    </Card>
    <Card elevation={2} style={{ padding: 16 }}>
        <p>Elevation 2 is generally for popovers</p>
    </Card>
    <Card elevation={3} style={{ padding: 16 }}>
        <p>Elevation 3 is for a fixed header (Working with UX to see if we need this)</p>
    </Card>
    <Card elevation={4} style={{ padding: 16 }}>
        <p>Elevation 4 is for Modals and Dialogs</p>
    </Card>
</div>
```
